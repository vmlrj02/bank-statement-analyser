pdfplumber>=0.11
pikepdf>=8
PyYAML>=6
openpyxl>=3.1

# LLM providers. All three are bundled so LLM_PROVIDER can be switched from the
# console without a redeploy; llm_providers.py imports lazily, so a provider you
# never select costs nothing at runtime.
google-genai>=1.0
anthropic>=0.40
