[pytest]
testpaths = tests
# tests/ imports the pipeline package (bsa.*) and loads each Lambda handler by
# path; conftest.py puts backend/processor on sys.path.
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
